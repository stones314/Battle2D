using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Inventory : Slot
{
    // Start is called before the first frame update
    void Start()
    {
        base.BaseStart();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
